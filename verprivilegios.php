<?php
		if($HTTP_COOKIE_VARS["tipo"]== 'admin') { $admin="ok"; }
		if($HTTP_COOKIE_VARS["equipe"]== 1) { $equipe="ok"; }
		$usuario = $HTTP_COOKIE_VARS["usuario"];
		$avatarusu = $HTTP_COOKIE_VARS["avatar"];		
?>